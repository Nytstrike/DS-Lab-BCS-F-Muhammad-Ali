#include <iostream>
using namespace std;

int sumTail(int n, int total) {
    if (n == 0) return total;
    
    return sumTail(n - 1, total + n);
}

int main() {
    int n = 5;

    cout << "Tail Recursion: Sum from 1 to " << n << " = " << sumTail(n, 0) << endl;
    return 0;
}
